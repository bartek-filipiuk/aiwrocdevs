---
interface Props {
  content: string;
  signupLink: string;
  expectFeature1Title: string;
  expectFeature1Description: string;
  expectFeature2Title: string;
  expectFeature2Description: string;
  expectFeature3Title: string;
  expectFeature3Description: string;
  joinHeading: string;
  joinSubtitle: string;
  joinButtonText: string;
}

const {
  content,
  signupLink,
  expectFeature1Title,
  expectFeature1Description,
  expectFeature2Title,
  expectFeature2Description,
  expectFeature3Title,
  expectFeature3Description,
  joinHeading,
  joinSubtitle,
  joinButtonText
} = Astro.props;
---

<section class="section info-section">
  <div class="container">
    <div class="info-content">
      <h2>What to <span class="gradient-text">Expect</span></h2>
      <div class="info-text">
        {content.split('\n').map(paragraph => paragraph.trim() && <p>{paragraph}</p>)}
      </div>

      <div class="features-grid">
        <div class="feature-card">
          <div class="feature-icon">
            <svg xmlns="http://www.w3.org/2000/svg" width="32" height="32" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
              <path d="M17 21v-2a4 4 0 0 0-4-4H5a4 4 0 0 0-4 4v2"></path>
              <circle cx="9" cy="7" r="4"></circle>
              <path d="M23 21v-2a4 4 0 0 0-3-3.87"></path>
              <path d="M16 3.13a4 4 0 0 1 0 7.75"></path>
            </svg>
          </div>
          <h3>{expectFeature1Title}</h3>
          <p>{expectFeature1Description}</p>
        </div>

        <div class="feature-card">
          <div class="feature-icon">
            <svg xmlns="http://www.w3.org/2000/svg" width="32" height="32" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
              <polygon points="12 2 2 7 12 12 22 7 12 2"></polygon>
              <polyline points="2 17 12 22 22 17"></polyline>
              <polyline points="2 12 12 17 22 12"></polyline>
            </svg>
          </div>
          <h3>{expectFeature2Title}</h3>
          <p>{expectFeature2Description}</p>
        </div>

        <div class="feature-card">
          <div class="feature-icon">
            <svg xmlns="http://www.w3.org/2000/svg" width="32" height="32" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
              <path d="M12 20h9"></path>
              <path d="M16.5 3.5a2.121 2.121 0 0 1 3 3L7 19l-4 1 1-4L16.5 3.5z"></path>
            </svg>
          </div>
          <h3>{expectFeature3Title}</h3>
          <p>{expectFeature3Description}</p>
        </div>
      </div>
    </div>
  </div>
</section>

<section id="signup" class="section signup-section">
  <div class="container">
    <div class="signup-card">
      <h2>{joinHeading}</h2>
      <p>{joinSubtitle}</p>
      <a href={signupLink} target="_blank" rel="noopener noreferrer" class="btn btn-primary btn-large">
        {joinButtonText}
        <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
          <line x1="5" y1="12" x2="19" y2="12"></line>
          <polyline points="12 5 19 12 12 19"></polyline>
        </svg>
      </a>
    </div>
  </div>
</section>

<footer class="footer">
  <div class="container">
    <div class="footer-content">
      <div class="footer-logo">
        <span class="gradient-text">AI WrocDevs</span>
        <p>Wrocław's AI & Technology Community</p>
      </div>
      <div class="footer-links">
        <a href="/about">About Us</a>
        <a href="/privacy">Privacy Policy</a>
      </div>
    </div>
    <div class="footer-bottom">
      <p>&copy; {new Date().getFullYear()} AI WrocDevs. All rights reserved.</p>
    </div>
  </div>
</footer>

<style>
  .info-section {
    background-color: var(--bg-primary);
  }

  .info-content h2 {
    text-align: center;
    margin-bottom: 2rem;
  }

  .info-text {
    max-width: 800px;
    margin: 0 auto 4rem;
    text-align: center;
  }

  .info-text p {
    color: var(--text-secondary);
    font-size: 1.1rem;
    line-height: 1.8;
    margin-bottom: 1rem;
  }

  .features-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
    gap: 2rem;
    margin-top: 3rem;
  }

  .feature-card {
    background-color: var(--bg-secondary);
    border: 1px solid var(--border-color);
    border-radius: 1rem;
    padding: 2.5rem;
    text-align: center;
    transition: all 0.3s ease;
  }

  .feature-card:hover {
    border-color: var(--accent-primary);
    transform: translateY(-8px);
    box-shadow: 0 12px 32px rgba(59, 130, 246, 0.2);
  }

  .feature-icon {
    width: 72px;
    height: 72px;
    margin: 0 auto 1.5rem;
    background: var(--accent-gradient);
    border-radius: 1rem;
    display: flex;
    align-items: center;
    justify-content: center;
    box-shadow: 0 4px 14px rgba(59, 130, 246, 0.3);
  }

  .feature-icon svg {
    color: white;
  }

  .feature-card h3 {
    color: var(--text-primary);
    margin-bottom: 0.75rem;
    font-size: 1.5rem;
  }

  .feature-card p {
    color: var(--text-secondary);
    line-height: 1.6;
  }

  .signup-section {
    background: linear-gradient(135deg, rgba(59, 130, 246, 0.15) 0%, rgba(139, 92, 246, 0.15) 100%);
  }

  .signup-card {
    max-width: 700px;
    margin: 0 auto;
    text-align: center;
    background-color: var(--bg-secondary);
    border: 1px solid var(--border-color);
    border-radius: 1.5rem;
    padding: 4rem 3rem;
    box-shadow: 0 12px 48px rgba(0, 0, 0, 0.3);
  }

  .signup-card h2 {
    margin-bottom: 1rem;
  }

  .signup-card p {
    color: var(--text-secondary);
    font-size: 1.2rem;
    margin-bottom: 2.5rem;
  }

  .btn-large {
    font-size: 1.1rem;
    padding: 1.25rem 2.5rem;
    display: inline-flex;
    align-items: center;
    gap: 0.75rem;
  }

  .btn-large svg {
    transition: transform 0.3s ease;
  }

  .btn-large:hover svg {
    transform: translateX(4px);
  }

  .footer {
    background-color: var(--bg-secondary);
    border-top: 1px solid var(--border-color);
    padding: 3rem 0 1.5rem;
  }

  .footer-content {
    display: flex;
    justify-content: space-between;
    align-items: center;
    margin-bottom: 2rem;
    padding-bottom: 2rem;
    border-bottom: 1px solid var(--border-color);
  }

  .footer-logo {
    font-size: 1.5rem;
    font-weight: 800;
  }

  .footer-logo p {
    font-size: 0.9rem;
    color: var(--text-secondary);
    font-weight: 400;
    margin-top: 0.25rem;
  }

  .footer-links {
    display: flex;
    gap: 2rem;
  }

  .footer-links a {
    color: var(--text-secondary);
    font-size: 0.95rem;
    transition: color 0.3s ease;
  }

  .footer-links a:hover {
    color: var(--text-primary);
  }

  .footer-bottom {
    text-align: center;
  }

  .footer-bottom p {
    color: var(--text-secondary);
    font-size: 0.9rem;
  }

  @media (max-width: 768px) {
    .features-grid {
      grid-template-columns: 1fr;
    }

    .signup-card {
      padding: 3rem 2rem;
    }

    .footer-content {
      flex-direction: column;
      gap: 2rem;
      text-align: center;
    }

    .footer-links {
      flex-direction: column;
      gap: 1rem;
    }
  }
</style>
